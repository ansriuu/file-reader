# file-reader
An small c project which reads the file you input and outputs on terminal


this project was initially made with using lyrics of songs saved in files to be displayed, so the output that you get will have a delay of 100ms.

